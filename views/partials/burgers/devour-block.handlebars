 <li>
    {{burger_name}}
  <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
   <div class="devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}{{else}}<button>Devour it</button>{{/if}}
   </div>
    </li>

   